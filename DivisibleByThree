// Write a method public static void divisibleByThreeInRange(int beginning, int end) 
// that prints all the numbers divisible by three in the given range. 
// The numbers are to be printed in order from the smallest to the greatest.

import java.util.Scanner;

public class DivisibleByThree {

    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        
        int int1 = Integer.valueOf(scanner.nextLine());
        int int2 = Integer.valueOf(scanner.nextLine());
        
        divisibleByThreeInRange(int1, int2);
    }

    // implement the method here
    // this method can by copypasted from mooc.fi
    public static void divisibleByThreeInRange(int beginning, int end) {
        for(; beginning <= end; beginning++) {
            if (beginning % 3 == 0) {
                System.out.println(beginning);
            }
        }           
    }
}
// had to change divisibleByThreeInRange(beginning, end); to what it is now
// had to add {} to the if statement. DUH
// had to change if (beginning % end == 3) to what it is now


